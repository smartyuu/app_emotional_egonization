## app_emotional_egonization 技术选型（Node.js + Python 双服务）

### 1. 项目概览

- **项目名称**：`app_emotional_egonization`
- **产品定位**：基于生理信号（EEG/ECG）+ AI 专家系统的「智能情感共鸣日记 App」。
- **核心价值**：
  - 引入客观生理数据，克服传统情绪打卡只依赖主观选择的局限。
  - 通过 GNN+CNN 情绪识别模型 + 大模型专家系统，为用户提供温柔、有共情感的心理建议。
- **设计风格**：
  - 卡片式 / 时间轴 / 仿纸质或流体渐变 UI。
  - 视觉强调「沉浸感」「私密性」，刻意避免 Dashboard 式的冰冷数据面板。

---

### 2. 整体架构概览

采用 **Node.js + Python 双服务架构**：

- **移动客户端（React Native App）**：负责日记编辑/展示、生理数据上报、情绪印章与专家回信展示、情绪轨迹可视化。
- **Node.js 业务网关服务**：负责用户体系、鉴权、业务接口（包括日记 CRUD、情绪轨迹接口等），对外暴露统一 API，并作为 Python AI 服务的调用方。
- **Python AI 推理服务**：负责 GNN+CNN 情绪识别、LLM 专家系统调用与封装，对外提供统一的 AI 推理接口。
- **数据与存储层**：关系型数据库 + 对象存储 + 可选缓存。

---

### 3. 前端技术栈（React Native App）

- **框架**：React Native CLI
  - 原因：便于后续接入原生蓝牙 BLE 模块，支持 ECG/EEG 硬件、HealthKit/Google Fit 等。
- **语言**：TypeScript
- **UI 与设计**：
  - 组件库：`react-native-paper` 或 `react-native-elements` 作为基础组件。
  - 自定义主题：莫兰迪色系、卡片式布局、时间轴/日历式情绪展示。
  - 强调「沉浸感」「私密性」，避免复杂图表感。
- **导航与路由**：
  - `@react-navigation/native`（Stack + Bottom Tabs）
  - 典型页面：登录/引导页、首页情绪流、写日记页、专家回信详情页、情绪轨迹页、设置页。
- **状态管理**：
  - 首选：`Zustand`（轻量、易上手）。
  - 如团队规模增大或状态复杂，可升级为 `Redux Toolkit`。
- **网络请求**：
  - `axios` 封装请求层，统一处理 Token、错误码、重试等逻辑。
- **图表与可视化**：
  - `react-native-svg` + `victory-native` 或 `react-native-svg-charts`。
  - 用于情绪折线图、情绪日历（色块深浅表示情绪强度）。
- **适配与布局**：
  - `react-native-responsive-fontsize`。
  - 百分比/弹性布局，适配手机、折叠屏、平板。

---

### 4. 后端技术栈与服务划分

#### 4.1 Node.js 业务网关服务

- **主要职责**：
  - 对移动端提供统一 REST API。
  - 负责用户体系、鉴权、订阅/付费（未来）、节流与速率限制。
  - 处理日记 CRUD、情绪轨迹查询等常规业务逻辑。
  - 调用 Python AI 服务完成情绪识别与专家回信生成。

- **技术选型**：
  - 运行环境：Node.js LTS（当前可选 20+）。
  - 框架：NestJS（推荐）或 Express。
    - NestJS 优点：模块化、依赖注入、装饰器风格，适合中长期维护。
  - 鉴权与安全：
    - JWT（Access Token + Refresh Token）。
    - 基于角色/权限的访问控制（为未来社区/管理后台预留）。
  - 数据访问：
    - ORM：Prisma 或 TypeORM（与 PostgreSQL/MySQL 集成）。
  - 日志与监控：
    - `winston` / `pino` 记录结构化日志。
    - 按需接入 Prometheus/Grafana（后期）。

- **与 Python 服务的通信方式**：
  - 初期：HTTP(s) REST 调用（简单直接，易调试）。
  - 未来可升级：gRPC 或消息队列（如 RabbitMQ / Kafka）以提升吞吐与解耦性。

#### 4.2 Python AI 推理服务

- **主要职责**：
  - 封装 GNN+CNN 情绪识别模型：
    - 接受预处理后的 ECG/EEG 数据（或文件），输出情绪标签及置信度。
  - 封装 LLM 专家系统调用：
    - 接收日记文本 + 情绪标签 + 近期情绪上下文。
    - 调用商用大模型 API，生成 100–200 字、具备共情与 CBT 风格的回复。
  - 将两者合并为统一的 AI 接口供 Node.js 调用。

- **技术选型**：
  - 语言：Python 3.11+。
  - Web 框架：FastAPI。
  - 推理引擎：
    - 模型格式：ONNX。
    - 推理：ONNX Runtime（开发期可直接在 FastAPI 内加载）。
    - 未来扩展：可考虑 Triton Inference Server 以支持更多模型与高并发。
  - LLM 调用：
    - 初期：商用 API（DeepSeek / 智谱 / OpenAI 等）。
    - 通过统一封装层隔离不同厂商 SDK。
  - 日志与监控：
    - `loguru` / `structlog` 输出结构化日志。
    - 记录请求 ID，以便与 Node.js 请求日志关联。

- **AI 接口示例（对外暴露给 Node.js）**：
  - `POST /emotion/infer`：对单次 ECG/EEG 数据进行情绪分类。
  - `POST /llm/reply`：根据日记与情绪标签生成专家回信。
  - `POST /pipeline/journal`：一站式接口：接收日记 + 生理数据 → 返回情绪标签 + 专家回信。

---

### 5. 数据与存储层

- **数据库**：
  - 选型：PostgreSQL（推荐）或 MySQL。
  - 存储内容：
    - 用户：账户信息、基础偏好设置。
    - 日记：文本内容、图片 URL、创建/修改时间、关联情绪标签等。
    - 情绪结果：时间戳、情绪标签、置信度、生理数据引用 ID。
    - LLM 调用日志：用于后续微调与效果分析（需脱敏/摘要化）。

- **对象存储**：
  - 内容：
    - 日记图片。
    - 生理原始数据文件（若不便直接存 DB）。
  - 选型：
    - 开发期：本地文件系统或 MinIO。
    - 生产：S3 兼容存储（阿里云 OSS、AWS S3 等）。

- **缓存层（可选，MVP 非必需）**：
  - Redis：
    - 缓存用户常用数据（如最近一周情绪统计）。
    - 存放短期的 LLM 响应，减少相同 Prompt 重复调用。

- **加密与隐私**：
  - 对生理数据和日记内容进行字段级加密（如 AES）。
  - 日志中严禁记录原始文本与原始波形，仅记录 ID、标签、摘要。

---

### 6. 安全与合规考量

- **数据敏感性**：
  - 生理数据（ECG/EEG）与日记文本均归类为高度敏感隐私数据。
  - 严格控制访问权限，区分开发/测试/生产环境数据。

- **合规定位**：
  - 在产品文案、用户协议中明确声明：本 App 为「情绪陪伴与健康辅助工具」，不提供医疗诊断或治疗建议。
  - 对可能触及心理危机的场景，仅提供热线/专业机构链接与提醒。

---

### 7. MVP 范围（2–3 个月目标，Node.js + Python 架构下）

- **客户端（RN App）**：
  - 实现日记 CRUD，采用温暖色调 UI。
  - 支持将日记文本与模拟/简单心电数据上传给后端。
  - 展示来自后端的情绪印章与专家回信。
  - 基础情绪轨迹页（简单折线或日历）。

- **Node.js 业务服务**：
  - 基础用户体系（可从单用户/简化登录开始）。
  - 日记与情绪结果的业务接口。
  - 与 Python AI 服务的 HTTP 调用打通。

- **Python AI 服务**：
  - 封装现有 GNN+CNN 模型为 `/emotion/infer` 接口。
  - 封装 LLM API 为 `/llm/reply` 接口，并内置统一 System Prompt。
  - 提供一站式 `/pipeline/journal` 接口供 Node.js 调用。

- **全链路验证**：
  - 流程：写日记 → 上传生理数据与文本 → Node.js 调用 Python → 情绪分类 + LLM 回信 → App 展示。
  - 基本性能测试：确保上传生理数据与模型推理不会明显卡顿前端体验。

---

### 8. 后续演进方向（超出 MVP）

- 上线匿名「情绪共振岛」社区，仍由 Node.js 负责业务与内容推荐。
- 引入 EEG 实时蓝牙流解析与可视化。
- 从商用 LLM 迁移到自建/微调 Qwen/Llama 模型（LoRA/QLoRA）。
- 完善监控、报警、灰度发布与 A/B 测试体系。

